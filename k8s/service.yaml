apiVersion: v1
kind: Service
metadata:
  name: fanda-fe-service
  namespace: fanda-fe
  labels:
    app: fanda-fe
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: fanda-fe
